import { ChevronLeft, ChevronRight } from "lucide-react";
interface Props { currentPage: number; totalPages: number; pageSize: number; totalRecords: number; setPageSize: (value: number) => void; setCurrentPage: (value: number | ((page: number) => number)) => void; }
export default function LeadPagination({ currentPage, totalPages, pageSize, totalRecords, setPageSize, setCurrentPage }: Props) {
  const allLeads = pageSize === 0; const start = totalRecords === 0 ? 0 : allLeads ? 1 : (currentPage - 1) * pageSize + 1; const end = allLeads ? totalRecords : Math.min(currentPage * pageSize, totalRecords);
  return <div className="flex flex-col gap-4 border-t border-zinc-100 bg-zinc-50/70 px-5 py-4 lg:flex-row lg:items-center lg:justify-between"><div className="flex items-center gap-3 text-sm text-zinc-500"><span>Rows</span><select value={pageSize} onChange={(event) => { setPageSize(Number(event.target.value)); setCurrentPage(1); }} className="rounded-lg border border-zinc-200 bg-white px-3 py-2 text-zinc-800"><option value={10}>10</option><option value={25}>25</option><option value={50}>50</option><option value={100}>100</option><option value={500}>500</option><option value={0}>All ({totalRecords})</option></select></div>{!allLeads && <><p className="text-sm text-zinc-500">Showing <b className="text-zinc-800">{start}–{end}</b> of <b className="text-zinc-800">{totalRecords}</b> leads</p><div className="flex items-center gap-2"><span className="mr-2 text-sm text-zinc-500">Page {currentPage} of {totalPages}</span><button aria-label="Previous page" disabled={currentPage === 1} onClick={() => setCurrentPage((page) => page - 1)} className="rounded-lg border border-zinc-200 bg-white p-2 text-zinc-700 hover:border-red-200 hover:text-[#e31e24] disabled:cursor-not-allowed disabled:opacity-40"><ChevronLeft size={17}/></button><button aria-label="Next page" disabled={currentPage >= totalPages} onClick={() => setCurrentPage((page) => page + 1)} className="rounded-lg border border-zinc-200 bg-white p-2 text-zinc-700 hover:border-red-200 hover:text-[#e31e24] disabled:cursor-not-allowed disabled:opacity-40"><ChevronRight size={17}/></button></div></>}</div>;
}
